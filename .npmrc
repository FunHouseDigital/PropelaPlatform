# eslint@10 conflicts with eslint-plugin-import@2.32.0's peer range (eslint <=9),
# which breaks a default `npm install` (ERESOLVE) on Vercel/CI. Allow legacy peer
# resolution so installs succeed everywhere until the lint deps are upgraded.
legacy-peer-deps=true
